/**
 * main.cpp
 * RD_DSP standalone CLI demo.
 *
 * Placeholder entry point — links against the RD_DSP static library.
 * Use this target for ad-hoc DSP experiments outside any plugin framework.
 */

#include <cstdio>

int main(int /*argc*/, char* /*argv*/[])
{
    std::printf("RD_DSP standalone — placeholder.\n");
    return 0;
}
